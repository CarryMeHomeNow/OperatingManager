eureka:
  server:
    response-cache-update-interval-ms: 1000
    eviction-interval-timer-in-ms: 1000

  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/
logging:
  level: info
  file: /var/log/uf/eureka/eureka.log
