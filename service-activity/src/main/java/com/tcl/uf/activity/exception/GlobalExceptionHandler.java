package com.tcl.uf.activity.exception;


//@ControllerAdvice
public class GlobalExceptionHandler {

	/*private static final Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class);

	@Value("${spring.application.name}")
	private String serviceName;

	@ResponseBody
	@ExceptionHandler(Exception.class)
	public ResponseData<Object> handleException(Exception e) {
		log.error(ExceptionStackTraceUtil.getStackTrace(e));
		return new ResponseData<Object>(-1, "服务器异常,请联系管理员,微服务(" + serviceName + ")", "The server is abnormal, please contact the administrator, Microservice is " + serviceName + "");
	}*/
}
